<PlaceButton>
    source: self.normal_image
    size_hint: 0, 0
    pos: self.parent.pos
    allow_stretch: True
    keep_ratio: False
    on_press: self.press()
    on_release:
        self.release()
        app.root.next_turn(self.number)

