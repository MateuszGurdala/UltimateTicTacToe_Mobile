<PlaceButton>
    source: self.normal_image
    size_hint: 0, 0
    pos: self.parent.pos
    allow_stretch: True
    keep_ratio: False
    on_press:
        self.on_click()
        self.source = app.root.current_sign

